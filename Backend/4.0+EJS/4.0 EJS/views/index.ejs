<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=<device-width>, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1> Hey is it a weekday? <%= weekDay %> </h1>
    <% if (weekDay) { %>
        <h3> It's a weekday! </h3>
    <% } else { %>
        <h3> It's weekend! </h3>
    <% } %>

    <h3> <%= advice %> </h3>
</body>
</html>